// export {checkCard}

// const cardAnimated = document.querySelectorAll('.gallery-item');

// window.addEventListener('scroll', checkCard)

// function checkCard() {
//     const triger = (window.innerHeight / 5) * 4;
//     for (const card of cardAnimated) {
//         const topOfBox = card.getBoundingClientRect().top;
//         if (topOfBox < triger) {
//             card.classList.add('show')
//         } else {
//             card.classList.remove('show')
//         }
//     }
// }